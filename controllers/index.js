const userController = require('./usersController');
const animesController = require('./animesController');
const chaptersController = require('./chaptersContoller');

module.exports = {
    userController, 
    animesController,
    chaptersController
}